#!/usr/bin/env bash
#
# java-globals.sh — Extra JVM tools via SDKMAN (optional).
#
# Invoked by:  scripts/install.sh, scripts/update.sh when sdk install lines exist below
# Prerequisite: SDKMAN + Java from install.sh (pin in lang/.tool-versions)
# Not stowed:  Repo only — see lang/.stow-local-ignore.
#
# Add active sdk install commands (examples commented out):
#
# sdk install gradle
# sdk install maven
